import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    authorHeading: {
      fontWeight: 'bold',
      fontSize: 14,
      paddingBottom: 10,
    },
  });

export default createStyles;
