import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    blogTitleInput: {
      width: '100%',
      height: '5%',
      borderRadius: 4,
      borderWidth: 1,
      borderColor: 'gray',
      marginBottom: 20,
      marginTop: 5,
      paddingHorizontal: 5,
    },
  });

export default createStyles;
