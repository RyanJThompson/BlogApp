import { StyleSheet } from 'react-native';

const createStyles = () =>
  StyleSheet.create({
    subtitleText: {
      fontWeight: 'normal',
      fontSize: 14,
      marginTop: 10,
    },
  });

export default createStyles;
